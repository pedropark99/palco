
/**
 * Unit test util to display an image.
 *
 * This function is intended to easily start a OpenGL Window,
 * and draw an arbitrary image into this window, so that you
 * can easily and quickly see the image data you have.
 *
 * This function is intended to be used once in a while in
 * an unit test, just to quickly see the results.
 *
*/
int display_image_data(unsigned char* data, int width, int height);
